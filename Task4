#include <iostream>
using namespace std;

int main() {

int number;

cout << "Введіть число" << endl;
cin >> number;

if (number % 2 == 0) {
    cout << number << "Парне" << endl;
} else {
    cout << number << "Непарне" << endl;
}
}
